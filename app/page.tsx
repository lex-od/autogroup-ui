'use client';

import PublicRoute from '@/components/core/public-route';

const HomePage = () => {
  return (
    <PublicRoute>
      <div>HomePage</div>
    </PublicRoute>
  );
};

export default HomePage;
